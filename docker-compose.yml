services:
  realm-web:
    build: .
    container_name: realm-web
    network_mode: host
    restart: unless-stopped
    environment:
      AUTH_USERNAME: admin
      AUTH_PASSWORD: password
      DATA_DIR: /app/data
      PANEL_PORT: "888"
    volumes:
      - ./data:/app/data
